## Creating the grid-test application

        $ mkdir grid_test && cd grid_test
        $ touch CMakeLists.txt grid.cpp
        $ mkdir build && cd build
        $ cmake ..
        $ cmake --build .
        $ ./grid
